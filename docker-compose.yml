services:
  iotabot:
    privileged: true
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8090:8090"
      - "80:80"
    volumes:
      - '/db_data:/db_data'
    container_name: iotabot
    restart: always
    depends_on:
      - db

  db:
    image: postgres:13.16-alpine
    volumes:
      - ./db_data:/var/lib/postgresql
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: osu
    container_name: db
    ports:
      - "5433:5432"

  pp_calc:
    build:
      context: ./pp_calculator
      dockerfile: Dockerfile
    ports:
      - "8091:8080"
    container_name: osu_tools
    restart: always


volumes:
  db_data:
